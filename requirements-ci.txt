# CPU CI deps — no torch / transformers so GitHub Actions stays fast.
# Full model runs use requirements.txt locally.

pytest>=7.4.0
pandas>=2.0.0
evaluate>=0.4.0
rouge-score>=0.1.0
tqdm>=4.65.0
pyarrow>=12.0.0
datasets>=2.14.0
huggingface-hub>=0.23.2,<1.0
gradio>=4.44.0,<5
